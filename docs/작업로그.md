# Cloyee 작업 로그

---

## 2026.03.15 — 전체 작업 내역

### 오전 세션 1 — RLS + Google OAuth 게시

**완료 항목**

- Google OAuth 앱 게시 완료 (테스트 모드 → 프로덕션 단계)
- Supabase RLS 활성화 완료
  - sessions: 본인 데이터 + user_id=NULL 조회 허용
  - reviews: sessions 조인으로 소유권 확인
  - categories: authenticated + anon 모두 읽기 허용
- 전체 기능 테스트 완료 (로그인, 홈, 기록, 성장, 학습 대화 모두 정상)

**관련 파일**

- Supabase RLS 정책 (대시보드에서 직접 설정)
- Google Cloud Console OAuth 동의 화면

---

### 오전 세션 2 — 온보딩 + profiles 테이블 + 버그 수정

**완료 항목**

- profiles 테이블 생성 + RLS + 트리거 (handle_new_user)
- category_order 컬럼 추가
- 온보딩 페이지 3단계 구현 (`/onboarding`)
  - 1단계: 직군 선택
  - 2단계: 경력/레벨 선택
  - 3단계: 첫 학습 로드맵 설정
- `/study` LNB 누락 버그 수정
- `/study` 카테고리 개인화 완료 (#15 해결)
- roadmaps 테이블 생성 + RLS + sessions.roadmap_id 컬럼 추가
- `/study/new` 학습 로드맵 추가 페이지 신규 구현
- 학습 대화에서 roadmap_id 컨텍스트 연동

**profiles 테이블 스키마**

```
id (uuid), job_role (varchar), experience (varchar), level (varchar),
onboarding_done (boolean), created_at, updated_at, category_order (jsonb)
```

**Notion 문서 업데이트**

- Cloyee DB 설계 업데이트
- Cloyee 이슈 트래커 업데이트
- Cloyee PRD 업데이트

---

### 오후 세션 1 — 랜딩페이지 + 대시보드 분리

**완료 항목**

- `app/page.jsx` → 새 랜딩페이지 생성 (미로그인 전용, 로그인 시 /dashboard 리다이렉트)
- `app/dashboard/page.jsx` → 기존 홈 페이지 이동
- `components/landing/GoogleLoginButton.jsx` → Google OAuth CTA 클라이언트 컴포넌트 생성
- `middleware.js` → `/` 퍼블릭 경로 추가
- `layout.js` → `/`에서 사이드바 숨김 처리
- `Sidebar.jsx` → 홈 링크 `/` → `/dashboard` 변경
- onboarding, chat, study/new → `router.push("/")` → `/dashboard` 변경
- `auth/callback` → 로그인 후 기본 리다이렉트 `/` → `/dashboard` 변경

**동작 흐름**

- 비로그인 + `/` 접근 → 랜딩페이지 표시
- 로그인 + `/` 접근 → 서버에서 즉시 `/dashboard` 리다이렉트
- 로그아웃 → `/login` 이동

---

### 오후 세션 2 — Supabase Auth 다중 로그인 구현

**완료 항목**

- `src/app/login/page.jsx` 신규 생성
  - 탭 UI: 소셜 / 이메일 / 링크
  - Google + GitHub OAuth 버튼
  - 이메일+비밀번호: 계정 없으면 자동 signUp
  - Magic Link: signInWithOtp (token_hash 방식)
  - 게스트로 둘러보기 버튼 → `/dashboard`
- `src/components/common/Sidebar.jsx` 업데이트
  - 로그아웃 버튼 추가 (LogOut 아이콘)
  - 게스트 상태 UI: "게스트 모드" + "로그인하고 저장하기" 버튼
  - 모바일 탭바에 로그인/로그아웃 버튼 추가
- `src/middleware.js` 업데이트
  - `/my` 등 개인 경로만 차단
  - `/dashboard`, `/study`, `/history`, `/growth` → 게스트 허용
- `src/app/auth/callback/route.js` 업데이트
  - token_hash 방식 추가 (Magic Link 크로스 브라우저 대응)
  - code 방식 (OAuth) + token_hash 방식 (Magic Link) 모두 처리
- `src/app/study/chat/page.jsx` — reviews insert에 `user_id: userId ?? null` 추가
- `src/app/study/review/page.jsx` — reviews insert에 `user_id: userId ?? null` 추가

**DB 변경사항**

```sql
ALTER TABLE categories ADD COLUMN user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE sessions ADD COLUMN user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE reviews ADD COLUMN user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;
```

**Supabase 대시보드 설정**

- Email Templates > Magic Link 커스텀 (token_hash 방식 URL)
- Google Provider: Enabled ✅
- GitHub Provider: Enabled ✅

**GitHub OAuth App 설정**

- App명: InstructorCloyee
- Homepage URL: `https://cloyee.vercel.app`
- Authorization callback URL: `https://fycowopnxqjkpyjarwul.supabase.co/auth/v1/callback`
- ⚠️ 초기에 예시 URL 그대로였어서 Invalid Redirect URI 에러 발생 → 수정 완료

---

---

## 2026.03.16 — SSE 스트리밍 + 모바일 개선

### 세션 1 — SSE 스트리밍 적용

**완료 항목**

- AI 모델 → `claude-haiku-4-5` 전환 (개발/테스트용 저비용)
- SSE 스트리밍 적용 (text/event-stream)
  - ThinkingBubble 제거 → 실시간 청크 출력
  - DONE 이벤트로 메타데이터(score, is_complete 등) 전송
- 채팅 헤더 sticky 고정
- 로딩바 제거 (스트리밍으로 불필요)
- sessions insert 버그 수정 (category_id UUID 오류)
- 대시보드 중단된 학습 표시 정상화

**관련 파일**

- `src/app/api/chat/route.js`
- `src/app/study/chat/page.jsx`

### 세션 2 — 모바일 전수 검증 + 이슈 해결

**완료 항목**

- 모바일 10건 수정 (터치 영역, safe-area, 반응형 그리드 등)
- 이슈 14건 전부 해결
- AI 토큰 비용 및 최적화 전략 문서 작성 (`docs/AI 토큰 비용 및 최적화 전략.md`)

---

## 2026.03.17 — 학습 플로우 재설계 + 버그 수정

### 세션 1 — 학습 플로우 재설계

**완료 항목**

- 첫학습 / 이어서(복습 or 바로) 진입 분기 자동 판단 (sessions 기록 유무 기준)
- 방식 선택 UI — 대화형 / 선택형
- 선택형 4지선다 구현 (ChoiceButtons)
  - 선택 강조 + 나머지 흐려짐(opacity 0.35)
  - 300ms 딜레이 후 자동 전송
- 문항 카운트 진도율 ("N문항 학습 중", 1문항 이상 + 학습 중에만 표시)
- score 분리 — 학습 중 완전 숨김, 완료 시 assistant 메시지 평균으로 sessions.score 저장
- CompleteBanner — "오늘의 이해도 N점" 완료 시에만 공개
- sessionStorage 키 분리 — `cloyee_chat_{roadmapId|categoryId}`로 로드맵별 독립
- lastSession 조회 is_complete 필터 제거 → 중단 세션도 복습 분기 진입 허용
- 복습 요약 카드 — roadmaps.topic 우선 표시, score=0이면 "이해도 기록 없음"
- 대시보드 이어하기 섹션 완전 제거
- 로드맵 카드 상태 뱃지 — ⚪시작 전 / 🟡진행 중 / ✅완료 + 경과 시간
- 최근 학습(대시보드) / 기록 페이지 — is_complete=true 완료 세션만 표시
- 이어하기 카드, 복습 인사 메시지 — roadmaps.topic 우선 표시 통일

**관련 파일**

- `src/app/study/chat/page.jsx`
- `src/app/dashboard/page.jsx`
- `src/components/study/ChoiceButtons.jsx`

### 세션 2 — 버그 수정 + 교안 revert

**완료 항목**

- API messages 필터링 버그 수정 — `map(({ role, content }) => ...)` 로 extra 필드(score/feedback/isDone) 제거
- ChoiceButtons `/history` 이동 버그 수정 — choices && is_complete 동시 수신 시 이동 안 하도록 수정
- 교안(curriculum) 구현 revert
  - `/api/curriculum/generate` 라우트 제거
  - `CurriculumPanel.jsx` 제거
  - 챕터 인디케이터, 프로그레스 바, "다음 챕터로" 버튼 제거
  - `is_chapter_complete`, `chapter_progress` DONE 이벤트 필드 제거
  - ⚠️ roadmaps.chapters 컬럼은 DB에 유지 (v0.3 재사용 예정)

**관련 파일**

- `src/app/api/chat/route.js`
- `src/components/study/ChoiceButtons.jsx`

---

## 2026.03.22 — 학습 기록 페이지 리팩터링

### 세션 1 — history/page.jsx 개선

**완료 항목**

- 완료 / 학습 중 탭 분리 (`tabFilter: "complete" | "ongoing"`)
- 세션 삭제 기능 — AlertDialog 확인 후 reviews + sessions 순차 삭제
- 제목 인라인 편집 — DropdownMenu → Pencil 메뉴로 진입, Input 인라인 전환, onBlur 저장
- 학습 중 세션에 "이어서 학습하기" 링크 추가 (`/study?categoryId=...`)
- `roadmaps(topic)` 조인으로 로드맵 제목 우선 표시
- 날짜 필터 (이번 주 / 이번 달 / 전체)
- 카테고리 필터 (세션 데이터에서 동적 추출)

**관련 파일**

- `src/app/history/page.jsx`

---

## 2026.03.22 — UX 휴리스틱 평가 + 이슈 수정

### UX 평가 (Chrome MCP 브라우저 자동화)
- 사용자 여정 전체 검증 완료 (게스트 기준, https://cloyee.vercel.app)
- Nielsen 10 휴리스틱 기준 이슈 7건 발견 (🔴 2건, 🟡 5건)

### 수정 완료
- Fix 1: 게스트 NULL 세션 노출 차단 — dashboard/page.jsx, history/page.jsx
- Fix 2: 게스트 /study/new 접근 차단 + 로그인 유도 모달 — study/page.jsx
- Fix 3: 랜딩 CTA 게스트 링크 추가 — app/page.jsx
- Fix 4: /study/new 폼 Collapsible 경량화 — study/new/page.jsx
- Fix 5: /history 탭 제거, 완료 세션만 표시 — history/page.jsx

### 미해결 이슈
- #6: /growth streak 수치 불일치 가능성
- #7: 모바일 탭바 게스트/로그인 상태 항목 일관성

---

## 2026.03.22 — 토큰 최적화 + 대시보드 UI 개선

### 세션 3 — 히스토리 슬라이싱 + 대시보드 최근 학습 레이아웃

**완료 항목**

- `api/chat/route.js`: `messages.slice(-10)` 적용 — 최근 10턴만 Claude API 전송
- `api/review/route.js`: 동일하게 `slice(-10)` + `map({ role, content })` 필터링 추가
- 대시보드 "최근 학습" 카드 레이아웃 개선
  - 컨테이너: `space-y-2` → `flex flex-col gap-1.5`
  - 카드: `Card + CardContent` → border/rounded `div` (flat card 스타일)
  - 아이콘: 34×34 neutral 박스, `w-4 h-4 text-neutral-400` 고정
  - 뱃지: border 제거, `text-[11px]`, neutral/green 배경으로 통일
  - 날짜: `ChevronRight` 제거, `text-[11px] text-neutral-400` 단독 표시

**관련 파일**

- `src/app/api/chat/route.js`
- `src/app/api/review/route.js`
- `src/app/dashboard/page.jsx`

---

## 2026.03.22 — SessionCard 컴포넌트 통합 + 중단 세션 요약 기능

### 세션 4 — SessionCard 공통 컴포넌트 추출

**완료 항목**

- `src/components/common/SessionCard.jsx` 신규 생성 (공통 컴포넌트)
  - `showMenu` prop으로 편집/삭제 메뉴 표시 분기 (기본 false)
  - 아이콘: 34×34 neutral 박스, `w-4 h-4 text-neutral-400` 고정
  - 뱃지: `text-[11px]`, neutral/green 배경으로 통일
  - 점수 표시: score > 0 일 때만 색상 코딩 (green/yellow/red)
  - 제목 인라인 편집 / 삭제 메뉴 (showMenu=true 시 활성)
- `dashboard/page.jsx`: 인라인 JSX 44줄 → `<SessionCard />` 1줄로 교체
- `history/page.jsx`: 로컬 SessionCard 함수 제거, 불필요 import 13개 정리, 공통 컴포넌트로 교체

**관련 파일**

- `src/components/common/SessionCard.jsx` (신규)
- `src/app/dashboard/page.jsx`
- `src/app/history/page.jsx`

### 세션 5 — 중단 세션 복습 요약 기능

**완료 항목**

- `src/app/api/chat/summarize/route.js` 신규 생성
  - POST: messages 받아 Claude로 2~3문장 요약 생성 후 반환
  - max_tokens: 256 (경량), model: DEFAULT_MODEL (haiku)
- `pauseSession()` 수정 — 저장 전 `/api/chat/summarize` 호출
  - 요약 성공 시 `sessions.summary`에 저장
  - 요약 실패 시 `null`로 graceful 처리 (저장 자체는 정상 진행)
- 기존 `summary: null` → `summary: pauseSummary` (insert/update 모두 반영)
- 효과: "복습하기" 클릭 시 이전 학습 요약 2~3문장 표시

**관련 파일**

- `src/app/api/chat/summarize/route.js` (신규)
- `src/app/study/chat/page.jsx`

---

## 이전 이력 요약 (v0.1 완성까지)

| 날짜       | 주요 작업                                                    |
| ---------- | ------------------------------------------------------------ |
| 2026.03.14 | 이슈 #12~14 해결, MVP v0.1 완성, Vercel 배포                 |
| 2026.03.14 | 학습 기록 편집/삭제 기능, 요약 문서 생성 기능                |
| 2026.03.14 | Monaco Editor 코드 리뷰, 이어하기 기능, Google OAuth 기초    |
| 2026.03.15 | RLS 활성화, Google OAuth 게시, 온보딩, 랜딩페이지, 다중 인증 |
